defaults: # Do it better / differently
  - defaults/train
  - defaults/dataset/HTVD
  - defaults/model/slayer_cuba
  - _self_

mlflow:
  mlflow_url: http://127.0.0.1:5000/
  experiment_name: SLAYER
  run_name: slayer_cuba
