defaults:
  - infer
  - dataset: HTVD
  - model: slayer_cuba_simple_dense
  - _self_

processing:
  checkpoint: /home/piotr/evdad/outputs/train/HTVD_CLASSIFIER/2024-05-02_18-10-13/checkpoint_best.pt
  input_dir: null

dataset:
  reshape_spike: true
  data_is_label: false

hydra:
  job:
    chdir: true
  searchpath:
    - file:///${oc.env:HOME}/evdad/src/conf/defaults
    # - file:///${oc.env:HOME}/evdad/src/conf/experiemnts